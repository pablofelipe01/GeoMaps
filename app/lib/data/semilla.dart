/// Carga `assets/semilla/*.json` en la base la primera vez que la app arranca.
///
/// Existe para que el primer arranque en un potrero muestre algo: la lista de
/// proyectos y el catalogo de simbolos tienen que estar antes de que el
/// telefono haya hablado nunca con el backend.
class Semilla {
  // TODO: sembrarSiHaceFalta(AppDatabase)
}
