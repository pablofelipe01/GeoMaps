/// Inventario de lo que la app genero.
///
/// Una fila por entregable, con su ruta local siempre y su URL del bucket solo
/// despues de sincronizar. La ruta local no se borra al subir: el tecnico
/// tiene que poder volver a compartir el KML del lote estando sin senal.
class ArchivosRepository {
  // TODO: registrar(File, tipo, {trazado, waypoint})
  // TODO: pendientesDeSubir()
  // TODO: marcarSubido(codigo, urlArchivo)
}
