/// Elegir que sale y en que formato.
///
/// KML y KMZ para Google Earth, GPX para un GPS de mano o para topografia,
/// GeoJSON para QGIS, PDF para imprimir. Sale por `share_plus`, asi que en el
/// telefono se puede mandar por WhatsApp sin pasar por el backend: en campo
/// eso es lo que de verdad ocurre.
///
/// El archivo queda registrado en `Archivos` aunque se comparta sin red. La
/// URL del bucket se llena despues, al sincronizar.
class ExportarPage {
  // TODO
}
