/// La pantalla principal: el mapa.
///
/// Lo unico que no puede faltar nunca en ella es **donde estoy y que tan
/// confiable es ese punto**. El circulo de precision se dibuja siempre, no
/// como opcion: un punto azul sin su radio hace creer que el GPS sabe mas de
/// lo que sabe.
///
/// Lo demas son capas que se encienden y apagan desde `capas_panel.dart`.
class MapaPage {
  // TODO: FlutterMap con capa base + MBTiles + trazados + waypoints + posicion
  // TODO: boton de brujula (recentrar), que se apaga al arrastrar
  // TODO: barra inferior: marcar waypoint, iniciar trazado, medir
}
